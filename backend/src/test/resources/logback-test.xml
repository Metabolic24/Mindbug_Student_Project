<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%level] %msg%n</pattern>
        </encoder>

        <filter class="ch.qos.logback.core.filter.EvaluatorFilter">
            <evaluator class="org.metacorp.mindbug.configuration.LogFilter"/>
            <onMatch>DENY</onMatch>
        </filter>
    </appender>

    <appender name="SIFT" class="ch.qos.logback.classic.sift.SiftingAppender">
        <discriminator class="org.metacorp.mindbug.configuration.LoggerDiscriminator"/>
        <sift>
            <appender name="FILE-${loggerName}" class="ch.qos.logback.core.FileAppender">
                <file>test-log\${loggerName}.log</file>
                <append>true</append>
                <encoder>
                    <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level - %msg%n</pattern>
                </encoder>
            </appender>
        </sift>

        <filter class="ch.qos.logback.core.filter.EvaluatorFilter">
            <evaluator class="org.metacorp.mindbug.configuration.LogFilter"/>
            <onMatch>DENY</onMatch>
        </filter>
    </appender>

    <root level="debug">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="SIFT"/>
    </root>
</configuration>